[tool.kedro]
package_name = "previsao_eleitoral_desafio_ds"
project_name = "previsao_eleitoral_desafio_ds"
project_version = "0.18.4"

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
addopts = """
--cov-report term-missing \
--cov src/previsao_eleitoral_desafio_ds -ra"""

[tool.coverage.report]
fail_under = 0
show_missing = true
exclude_lines = ["pragma: no cover", "raise NotImplementedError"]
